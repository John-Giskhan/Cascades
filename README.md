# Cascades
Realtime Cascades is a data oriented C++ Vulkan engine for modern real-time rendering and gameplay creation. It combines a GPU-driven renderer with hot-reloadable game logic, a runtime editor, and a production-style asset pipeline around glTF.
